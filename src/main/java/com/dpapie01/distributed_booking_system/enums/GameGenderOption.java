package com.dpapie01.distributed_booking_system.enums;

/**
 * Enums represents game gender options for a game.
 * Gender eligibility rule for a game is checked against a player's Gender through Gender.isEligibleFor(GameGenderOption) method.
 */
public enum GameGenderOption {
    MEN,
    WOMEN,
    MIXED
}