package com.dpapie01.distributed_booking_system.enums;

/**
 * Enums represents role set for the User.
 * Determines authorisation and what the user is permitted to do/access on the system.
 */
public enum Role {
    PLAYER,
    ADMIN
}